<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">



<mapper namespace="com.app.dao.FabulousDao">
	
	<select id="queryFabulousCount" resultType="String">
		SELECT fabulousCount FROM t_fabulous WHERE articleId = #{0}
	</select>
	
	<insert id="investFabulous">
		INSERT INTO t_fabulous (articleId, userId, fabulousCount, fabulousDate) 
		VALUES (#{articleId}, #{userId}, #{fabulousCount}, #{fabulousDate})
	</insert>
	
</mapper>





